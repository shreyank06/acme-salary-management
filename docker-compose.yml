services:
  api:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      SALARY_CORS_ORIGINS: '["http://localhost:3000"]'
    volumes: ["salary-data:/data"]
  web:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8000
    ports: ["3000:3000"]
    depends_on: [api]
volumes:
  salary-data:
